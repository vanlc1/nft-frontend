import type { NextPage } from 'next'
import { CoreLayout } from 'layouts'

const MarketplacePage: NextPage = () => {
  return <CoreLayout maxW="container.lg">Marketplace</CoreLayout>
}

export default MarketplacePage
